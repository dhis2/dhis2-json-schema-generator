{
  "type": "object",
  "properties": {
    "enrollments": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uid",
        "minLength": 11,
        "maxLength": 11,
        "pattern": "^[a-zA-Z][a-zA-Z0-9]{10}$"
      }
    },
    "relationships": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uid",
        "minLength": 11,
        "maxLength": 11,
        "pattern": "^[a-zA-Z][a-zA-Z0-9]{10}$"
      }
    },
    "trackedEntityAttributes": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uid",
        "minLength": 11,
        "maxLength": 11,
        "pattern": "^[a-zA-Z][a-zA-Z0-9]{10}$"
      }
    }
  }
}
