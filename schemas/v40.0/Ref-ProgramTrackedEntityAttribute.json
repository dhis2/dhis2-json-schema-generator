{
  "type": "object",
  "required": [
    "id"
  ],
  "properties": {
    "id": {
      "type": "string",
      "format": "uid",
      "pattern": "^[0-9a-zA-Z]{11}$",
      "minLength": 11,
      "maxLength": 11,
      "example": "LwGL95uiZVa"
    }
  },
  "description": "A UID reference to a ProgramTrackedEntityAttribute  \n(Java name `org.hisp.dhis.program.ProgramTrackedEntityAttribute`)"
}
