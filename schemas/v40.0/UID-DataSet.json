{
  "type": "string",
  "format": "uid",
  "pattern": "^[0-9a-zA-Z]{11}$",
  "minLength": 11,
  "maxLength": 11,
  "example": "dTpjiNfGmSM",
  "description": "A UID for an DataSet object  \n(Java name `org.hisp.dhis.dataset.DataSet`)"
}
