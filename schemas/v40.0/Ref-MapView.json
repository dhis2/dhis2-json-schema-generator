{
  "type": "object",
  "required": [
    "id"
  ],
  "properties": {
    "id": {
      "type": "string",
      "format": "uid",
      "pattern": "^[0-9a-zA-Z]{11}$",
      "minLength": 11,
      "maxLength": 11,
      "example": "mTl8ENiPmOB"
    }
  },
  "description": "A UID reference to a MapView  \n(Java name `org.hisp.dhis.mapping.MapView`)"
}
