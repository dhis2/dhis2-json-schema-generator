{
  "type": "object",
  "required": [
    "dataMergeStrategy"
  ],
  "properties": {
    "dataMergeStrategy": {
      "$ref": "DataMergeStrategy.json"
    },
    "deleteSources": {
      "type": "boolean"
    },
    "sources": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uid",
        "minLength": 11,
        "maxLength": 11,
        "pattern": "^[a-zA-Z][a-zA-Z0-9]{10}$"
      }
    },
    "target": {
      "type": "string",
      "format": "uid",
      "minLength": 11,
      "maxLength": 11,
      "pattern": "^[a-zA-Z][a-zA-Z0-9]{10}$"
    }
  }
}
